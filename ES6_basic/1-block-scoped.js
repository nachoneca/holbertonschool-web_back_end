export default function taskBlock(trueOrFalse) {
    /* eslint-disable-next-line no-unused-vars */
      const unused = trueOrFalse;
      const task = false;
      const task2 = true;
    
      return [task, task2];
    }